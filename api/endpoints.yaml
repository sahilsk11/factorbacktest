# API Endpoint Definitions
# This file defines all API endpoints. Run `make generate-api` to:
# - Generate request/response Go structs
# - Generate resolver file stubs (if missing)
# - Update api.go router registration

endpoints:
  - path: /updateOrders
    method: POST
    handler: updateOrders
    requires_auth: false
    generate_resolver: false
    request:
    response:
      success: string

  - path: /sendSavedStrategySummaryEmails
    method: POST
    handler: sendSavedStrategySummaryEmails
    requires_auth: false
    request:
      # Empty object means no request body
    response:
      message: string

  # Example: Endpoint with request body (uncomment and customize as needed)
  # - path: /exampleEndpoint
  #   method: POST
  #   handler: exampleEndpoint
  #   requires_auth: true
  #   request:
  #     field1: string
  #     field2: int
  #     optionalField: string?  # ? means optional (pointer in Go)
  #   response:
  #     result: string
  #     count: int
