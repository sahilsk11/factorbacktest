; react-scripts 5 declares a peerOptional on typescript ^3 || ^4, but several
; of our deps (and typescript itself in devDependencies) require >= 5. The
; conflict is benign (react-scripts only uses TypeScript via fork-ts-checker,
; which accepts TS 5), so opt into legacy resolution to keep `npm ci` working.
legacy-peer-deps=true
