import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);
alert("Unfortunately EKS bills were racking up, so Factor Backtest is temporarily disabled! At least until I can move to serverless :)") 
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
